Коммуникация клиента с сервером:
	Клиент отсылает серверу один байт, в котором (битмаской) закодирован его ввод.

	Последовательность бит в порядке убывания: Вверх-вниз-влево-вправо-пробел-шифт-резерв-резерв. 

Коммуникация сервера с клиентом:
	Сервер отсылает клиенту JSON-объект, в котором могут быть следующие поля:

	'palette': массив URI картинок (например, 'resources/tilesets/destro.png', 'http://i.imgur.com/iQ2sRA7.jpg').
		Задают палитру блоков, используемую в игре.

	'field': двумерный прямоугольный массив чисел. Числа соответствуют номерам картинок из палитры.
		При получении этого объекта уровень генерируется заново, используя заданное поле как карту.

	'blocks': массив словарей с полями x, y и type, меняющих блок в координатах (x, y) на тип type.

	'entities': поле, отвечающее за объекты на уровне. Это словарь, в котором каждому ключу соответствует словарь-действие 3d-объекта.

		Поля, которые могут быть в словаре-действии:
			
		'x', 'y' - координаты, в которые смещается объект

		'sprite' - URI спрайта, который использует объект

		'dir' - Модуль значения: направление, в которое объект поворачивается (в градусах), знак - направление поворота объекта(по/против ч.с.)

		'vel' - значение, определяющее, за сколько миллисекунд обьект должен достичь конечного состояния(позиции)

		'dirvel' - аналогично, но с направлением

		'draw' - булево значение, определяющее, отрисовывать ли объект или нет.

	Названия объектов, начинающиеся на точку, зарезервированы игровым движко. Использовать их как названия своих объектов не рекомендуется.

	'sounds': массив URI звуков. Звуки, заданные в этом поле, прозвучат после получения пакета.

	'screenshake': число - интенсивность скриншейка.

	'preload': объект, указывающий на ресурсы, которые стоит загрузить заранее. Может содержать поля:

		'images' - изображения, требующие предварительной загрузки

		'sounds' - звуки, требующие предварительной загрузки

	'scroll': булево значение, позволяющее (при установлении в false) остановить прокрутку за счёт ускорения самой игры
